[pytest]
asyncio_mode = auto
markers =
    llm_live: real LLM/MCP integration tests (slow, needs network)
    gate: functional gate tests
    unit: unit tests
    integration: integration tests
    e2e: end-to-end tests
